functions:
  row_to_message_with_timestamp:
    schema_name: null
    template: "{}"
    log_time_template: "{{ (timestamp * 1_000_000_000) | int }}"
    publish_time_template: "{{ (timestamp * 1_000_000_000) | int }}"
